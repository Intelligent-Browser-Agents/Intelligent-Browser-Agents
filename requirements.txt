# Runtime dependencies.
#
# Derived from the imports actually present in backend/, and pinned to the
# versions this tree is verified against. Development-only tooling lives in
# requirements-dev.txt.
#
# Install:
#   pip install -r requirements.txt
#   python -m playwright install chromium

# Web server
fastapi==0.135.1
uvicorn[standard]==0.42.0

# Auth, database, and credential-vault encryption
bcrypt==5.0.0
cryptography==46.0.5
psycopg2-binary==2.9.11
pyjwt==2.12.1

# Agent framework and model providers.
# langchain-openai is required: every agent in backend/src/models.py is assigned
# an OpenAI model. langchain-google-genai is required only if AGENT_MODELS is
# switched to a "gemini-*" key.
langchain-core==1.2.22
langchain-openai==1.1.12
langchain-google-genai==4.2.1
langgraph==1.1.3

# Browser automation and page parsing
beautifulsoup4==4.14.3
playwright==1.58.0

# Configuration and validation
pydantic==2.12.5
python-dotenv==1.2.2
pyyaml==6.0.3

# Optional provider. models.py::_get_llm_class has an "anthropic" branch, but no
# Anthropic entry is enabled in MODELS, so the package is not installed by
# default. Uncomment together with a MODELS entry to use it.
# langchain-anthropic==1.4.0
