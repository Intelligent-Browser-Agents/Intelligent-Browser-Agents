# AGENTS.md

## Project Rules
- Backend code lives in `backend/`
- Skills live in `.agents/skills/`
- MVP for this project lives in [INSERT FILE PATH WHEN UPLOADED]
- Prefer `rg` for search
- Run backend from `backend/server.py`

## Skill Conventions
- Each skill must be in `.agents/skills/<name>/SKILL.md`
- Each `SKILL.md` should include frontmatter with `name` and `description`
- Keep skill instructions concrete and task-focused
